# Test env. Pins the app to the in-memory mock API so unit tests never depend
# on a running backend, even when a developer has VITE_USE_MOCK_API=false in
# their gitignored .env.local. Mode-specific files override .env.local in Vite.
VITE_USE_MOCK_API=true
