// Placeholder app shell. Routing and screens are added in later commits;
// this keeps the scaffold compiling and gives us a smoke-testable root.
export default function App() {
  return (
    <main className="flex min-h-full items-center justify-center p-6">
      <div className="text-center">
        <h1 className="text-2xl font-semibold text-brand">Paradox Connect</h1>
        <p className="mt-2 text-muted">Sprint 1 frontend — setup in progress.</p>
      </div>
    </main>
  );
}
